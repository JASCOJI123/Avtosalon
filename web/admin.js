async function login(){let r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams({username:u.value,password:p.value})});if(r.ok){document.querySelector('#login').hidden=true;document.querySelector('#panel').hidden=false;load()}else alert('Login yoki parol xato')}async function load(){let s=await fetch('/api/admin/stats');if(!s.ok)return;let x=await s.json();stats.innerHTML=`<div class="card">👥 Leadlar: <b>${x.leads}</b></div><div class="card">📅 Test-drive: <b>${x.test_drives}</b></div><div class="card">🚘 Mashinalar: <b>${x.vehicles}</b></div>`;let l=await fetch('/api/admin/leads').then(r=>r.json());leads.innerHTML=l.map(x=>`<div class="card"><b>#${x.id} ${x.name}</b><br>📞 ${x.phone}<br>🚘 ID: ${x.vehicle_id||'-'}<br>Status: <select onchange="setStatus(${x.id},this.value)"><option ${x.status=='new'?'selected':''}>new</option><option ${x.status=='contacted'?'selected':''}>contacted</option><option ${x.status=='test_drive'?'selected':''}>test_drive</option><option ${x.status=='sold'?'selected':''}>sold</option><option ${x.status=='lost'?'selected':''}>lost</option></select></div>`).join('')||'Lead yo‘q'}async function setStatus(id,status){await fetch(`/api/admin/leads/${id}?status=${status}`,{method:'PATCH'});load()}async function addCar(){let data={brand:brand.value,model:model.value,price:+carprice.value,year:+year.value,description:desc.value};let r=await fetch('/api/admin/vehicles',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(r.ok){alert('Qo‘shildi');load()}else alert('Xatolik')}async function logout(){await fetch('/api/admin/logout',{method:'POST'});location.reload()}